# 知乎账号配置（可选，也可以手动在浏览器中登录）
ZHIHU_USERNAME=your_phone_or_email
ZHIHU_PASSWORD=your_password

# 浏览器配置
HEADLESS=false        # true = 无头模式（不显示浏览器窗口），false = 显示浏览器
SLOW_MO=100           # 操作间延迟（毫秒），数值越大越像人工操作

# Session 保存目录（默认为 .session/）
# USER_DATA_DIR=/custom/path/to/session
